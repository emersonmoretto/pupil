cmake_minimum_required(VERSION 2.8)
project( pupil )
find_package( OpenCV )
include_directories( ${OpenCV_INCLUDE_DIRS} )
add_executable( pupil pupil-detect.cpp )
target_link_libraries( pupil ${OpenCV_LIBS} )
